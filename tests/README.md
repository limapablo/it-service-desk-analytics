# Tests

Automated data-quality and transformation tests will be added as business rules are defined.

Examples planned for later phases:

- required columns are present;
- ticket IDs satisfy the chosen uniqueness rule;
- opening dates parse correctly;
- closing dates do not precede opening dates unless documented as a source-data issue;
- transformed datasets conform to the expected schema.
