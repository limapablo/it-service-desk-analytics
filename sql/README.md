# SQL

PostgreSQL scripts used in the analytical layer.

Planned files:

- `01_create_schema.sql` — database/schema objects and staging tables.
- `02_data_model.sql` — analytical/dimensional model.
- `03_kpis.sql` — reusable KPI views and calculations.
- `04_business_analysis.sql` — queries answering the project's business questions.

SQL implementation begins after the Python ingestion and validation stages produce a stable dataset.
